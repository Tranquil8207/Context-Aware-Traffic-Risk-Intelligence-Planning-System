ultralytics>=8.3.0
opencv-python>=4.10.0
fastapi>=0.115.0
uvicorn>=0.32.0
python-multipart>=0.0.12
supabase>=2.9.0
python-dotenv>=1.0.0

# objectdetection.py uses numpy directly for homography/velocity math.
# torch/torchvision/yacs were only needed for the now-removed YOLOP lane
# segmentation (superseded by manual lane boxing in calibrate_lanes.py).
numpy>=1.24.0
